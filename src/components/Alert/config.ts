import { AlertControl } from "@/components";

export const errorAlertDefaultConfig: AlertControl = {
    title: "Oh, no!",
    body: "We couldn't complete your request at the moment. Please try again.",
    show: false,
    theme: "error"
};
